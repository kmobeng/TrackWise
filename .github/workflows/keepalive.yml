name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app to prevent spin-down
        run: |
          curl -fsS --max-time 90 "${{ secrets.RENDER_URL }}/healthz" || echo "Ping failed"