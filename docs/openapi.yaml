openapi: 3.0.3
info:
  title: TrackWise API
  version: 1.0.0
  description: Expense tracking API with auth, budgets, and summaries.
servers:
  - url: /api/v1
    description: API v1
components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: accessToken
security:
  - cookieAuth: []
paths: {}
